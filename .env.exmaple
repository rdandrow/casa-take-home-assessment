# This file should be copied to .env and edited with the appropriate values where needed.
# The PLAYWRIGHT_BASE_URL should point to the URL where the app is running (e.g. http://localhost:3000).
# The PLAYWRIGHT_BASE_URL is an optional variable, and will default to https://app-stg.keys.casa if not set. 
PLAYWRIGHT_BASE_URL=